# How to Use Python and ChatGPT to Generate Custom Anki Decks

1. Install Anki if you don't already have it

`brew install anki`

2. Run the Makefile

clone this repository with `git clone x` and run `run make` in the root directory

3. Ensure your OpenAI API key is in your .bashrc/.zshrc file

4. 